let utils = {
    memory: require("./util.memory"),
    profiler: require("./util.profiler"),
    upgrader: require("./util.visual-room"),
    stats: require("./util.stats"),
};

module.exports = utils;
